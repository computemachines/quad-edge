[package]
name = "quad-edge"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
gui = ["bevy"]

[dependencies]
cgmath = "0.18.0"
bevy = { version = "0.6", optional = true }
